{
  "name": "@frameforge/engine-export",
  "version": "0.0.0",
  "private": true,
  "description": "逐幀擷取與 MP4 匯出（WebCodecs + mp4-muxer），與渲染器解耦",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mp4-muxer": "^5.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
